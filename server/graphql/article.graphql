# import BPInfo from "./bp.graphql"
# import PageInfo from "./pagination.graphql"

type Article {
  title: String!
  content: String!
  producerInfo: BPInfo
}
type ArticlesList {
  articles: [Article!]!
  pageInfo: PageInfo
}
